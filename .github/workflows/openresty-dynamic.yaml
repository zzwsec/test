name: openresty dynamic container build

# Reusable workflow inputs:
# https://docs.github.com/en/actions/how-tos/reuse-automations/reuse-workflows#using-inputs-and-secrets-in-a-reusable-workflow
on:
  workflow_call:
    inputs:
      image_name:
        required: true
        type: string
      resty_version:
        description: OpenResty source version to build
        required: true
        type: string
      alpine_image:
        required: true
        type: string
      openssl_version:
        required: true
        type: string
      pcre_version:
        required: true
        type: string

env:
  IMAGE_NAME: ${{ inputs.image_name }}
  ALPINE_IMAGE: ${{ inputs.alpine_image }}
  RESTY_VERSION: ${{ inputs.resty_version }}
  RESTY_OPENSSL_VERSION: ${{ inputs.openssl_version }}
  RESTY_PCRE_VERSION: ${{ inputs.pcre_version }}

permissions:
  contents: read

jobs:
  build_docker:
    # Dynamic modules are compiled natively for each target architecture.
    # https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/run-job-variations
    strategy:
      fail-fast: true
      matrix:
        include:
          - os: ubuntu-26.04
            platform: linux/amd64
            arch: amd64

          - os: ubuntu-26.04-arm
            platform: linux/arm64
            arch: arm64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          registry: docker.io
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      # The versioned static OpenResty image is the ABI base for dynamic modules.
      # Each platform is pushed by digest to avoid concurrent tag updates.
      # https://docs.docker.com/build/ci/github-actions/multi-platform/
      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v7
        with:
          context: openresty/dynamic
          file: openresty/dynamic/Dockerfile
          platforms: ${{ matrix.platform }}
          build-args: |
            ALPINE_IMAGE=${{ env.ALPINE_IMAGE }}
            RESTY_VERSION=${{ env.RESTY_VERSION }}
            RESTY_OPENSSL_VERSION=${{ env.RESTY_OPENSSL_VERSION }}
            RESTY_PCRE_VERSION=${{ env.RESTY_PCRE_VERSION }}
            OPENRESTY_IMAGE=docker.io/${{ vars.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ env.RESTY_VERSION }}
          outputs: type=image,name=docker.io/${{ vars.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }},push-by-digest=true,name-canonical=true,push=true

      - name: Export digest
        run: |
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "/tmp/digests/${digest#sha256:}"

      # https://docs.github.com/en/actions/tutorials/store-and-share-data
      - name: Upload digest
        uses: actions/upload-artifact@v7
        with:
          name: openresty-dynamic-digests-${{ matrix.arch }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  merge:
    runs-on: ubuntu-26.04

    # https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#jobsjob_idneeds
    needs:
      - build_docker

    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          registry: docker.io
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Download digests
        uses: actions/download-artifact@v8
        with:
          path: /tmp/digests
          pattern: openresty-dynamic-digests-*
          merge-multiple: true

      # Merge native image digests into the public multi-platform dynamic tag.
      # https://docs.docker.com/build/ci/github-actions/multi-platform/
      - name: Create manifest list and push
        working-directory: /tmp/digests
        run: |
          IMAGE="docker.io/${{ vars.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}"

          docker buildx imagetools create \
            -t "${IMAGE}:${{ env.RESTY_VERSION }}-dynamic" \
            $(printf "${IMAGE}@sha256:%s " *)

      - name: Inspect image
        run: |
          IMAGE="docker.io/${{ vars.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}"

          docker buildx imagetools inspect "${IMAGE}:${{ env.RESTY_VERSION }}-dynamic"
